# Digital Systems and Logic Designs 

## Logic Gates
### AND Gate
*insert image*
*make logical table*
 

Uses
- controlling incoming data stream
	- control one of the inputs and use "and" gate to get the other input as output whenever we want by changing between 0 and 1
	- This is basically a *switch* 
- Masking purpose
	- works same as the switch
	- masking an incoming signal or masking a particular bit (making a particular, bit say the last one always zero, like that), etc.